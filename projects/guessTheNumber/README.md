# Guess The Random Number Program
- creates a random number seed based on the current time
    - a = None
- generates a random number
- asks the user for input
- checks if the input is the same as the random number
- exits the program when the user has guessed correctly